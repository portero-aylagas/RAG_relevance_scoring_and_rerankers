This implementation now uses two sources: the English Trustworthy AI PDF and a transcript generated from the Trustworthy AI podcast audio. The Pinecone baseline retrieval already surfaces strong chunks for direct questions about trustworthy AI, human oversight, and transparency, while the LLM relevance scoring step makes the ranking easier to justify by adding a 0-5 grounded score, a combined score, and a short explanation for each chunk. Metadata filtering is useful for checking how the PDF and podcast behave separately: the PDF remains the strongest source for precise definitions, while the transcript is useful as a secondary explanatory source. For this lab scope, I would keep the relevance-scoring layer because it is easy to understand and demonstrates why a second-pass ranking step can improve trust in retrieval results.
